# semicircle-decay-sim

リアクションホイールを搭載した左右ロッキング機体について、実機の自由減衰と足裏形状を物理モデルで比較するためのリポジトリです。

## 目的

現在ファームウェアで使っているピーク間自由減衰モデルと、実際の足裏接触形状に基づく簡略物理モデルを比較します。

特に以下を切り分けます。

1. 完全な半円足を仮定したモデルが実機のゼロクロス近傍を正しく表しているか
2. STEP形状にある中央の円弧欠損が、振幅とゼロクロス角速度の関係にどの程度影響するか
3. 自由減衰で得たモデルをリアクションホイール再構成後へそのまま適用できるか
4. 足裏形状だけで説明できない差を、リアクションホイール状態・左右非対称・接触損失などへ分離できるか

## STEPから確認した座標系

Onshapeから書き出した `Part Studio 1.1.step` を形状参照として解析しています。CADの質量・密度・重心・慣性モーメントは使用しません。

- **X**: 実機の左右方向。正面動画の水平方向
- **Y**: 実機の前後方向。足裏円弧の円筒軸方向
- **Z**: 実機の鉛直方向
- 左右ロッキングは **Y軸回り**

動画の正面像はCADのXZ断面と対応します。

## 足裏形状の重要な発見

STEPの左右足裏外周は、共通の半径150 mmの円筒面です。

- 円弧半径: `R = 150 mm`
- 円筒軸: Y方向
- 円中心線: おおむね `X = 0`, `Z = 56.951 mm`
- 左足の円弧範囲: `X = -45 ... -5 mm`
- 右足の円弧範囲: `X = +5 ... +45 mm`
- 中央の欠損幅: **10 mm**

したがって完全円の最下点 `X=0` は実体として存在しません。

内側端点から円弧転がりへ移る幾何学角は

```text
theta_inner = asin(5 / 150) = 1.910 deg
```

外側端点は

```text
theta_outer = asin(45 / 150) = 17.458 deg
```

です。

このため理想化した接触モードは次のようになります。

```text
|theta| = 0
    左右の内側端点が同じ高さ

0 < |theta| < 1.910 deg
    低い側の内側端点を支点にロッキング

1.910 <= |theta| <= 17.458 deg
    R=150 mm の円弧上を転がる
```

> 注意: 実機動画では中央に白色の柔軟な部材が見えますが、STEPには荷重支持形状として含まれていません。この部材が実際に地面荷重を受ける場合は接触モデルを修正する必要があります。

## 完全円モデルとの差

これまで使ってきた単純な完全円モデルでは、直立時重心高さを `H=120 mm` とすると

```text
Delta h_old = (R - H) * (1 - cos(theta))
```

です。

中央欠損を含むモデルでは、`a=5 mm` として、中央端点支点領域では

```text
Delta h_gap = H*(cos(|theta|)-1) + a*sin(|theta|)
```

となります。

円弧領域では、直立時に内側端点が接地していることを考慮し

```text
hC0 = sqrt(R^2 - a^2)
d = hC0 - H
Delta h_gap = R - d*cos(|theta|) - H
```

とします。

`H=120 mm`, `m=199.7 g` を使うと、6.5--7 deg付近では完全円モデルより必要位置エネルギーが約37--43%大きくなります。

これは現在の固定Q状態ゲート（6.5 deg付近）に対して無視できない差です。

## 現時点の実測との比較

2026-08-24 V61 run 2の自由減衰区間について、動画ピーク角とIMUジャイロのゼロクロス角速度を使い、各側ごとに一定の有効慣性 `J` を1個だけフィットしました。

ゼロクロス角速度のRMSEは概略:

| 前のピーク側 | 完全円 | 中央欠損を含む形状 |
|---|---:|---:|
| 負側ピーク -> 正方向ゼロクロス | 8.67 deg/s | **3.28 deg/s** |
| 正側ピーク -> 負方向ゼロクロス | 4.31 deg/s | **1.60 deg/s** |

この比較では中央欠損を入れるだけでRMSEが約62--63%低下しました。

したがって、**完全円仮定がゼロクロス速度と振幅の関係を歪めていた可能性は高い**です。

一方、V61の再構成区間では、同じ実形状モデルで自由減衰から予測した速度に対して、正方向到着が平均約 `+9.3 deg/s`、負方向到着が平均約 `-12.0 deg/s`ずれる傾向が残ります。

よって中央欠損は重要ですが、**V61再構成後の不一致をこれだけで全て説明することはできません**。次段階ではリアクションホイールの残留回転・電流履歴、左右非対称、接触/滑りを状態として切り分けます。

## 参照方針

- `study_AT`: **参照専用**。このリポジトリからは書き換えない
- CAD/STEP: 形状・寸法だけに使用する
- 質量、重心、慣性: 実測値または実測ログから同定する

## Status

**Version 1 / geometry audit started**

次の実装:

- STEPから得た形状を再現するpiecewise rocker model
- 完全円モデルとのエネルギー比較
- V61自由減衰データからの有効パラメータ同定
- 実測時系列との重ね合わせ
- reaction-wheel stateを追加したpost-rebuild model
